import styled from "styled-components";

export const CalendarDataLayOut = styled.div`
  display: inline-flex;
  height: 36px;
  padding: 4px 12px;
  align-items: center;
  gap: 8px;
  flex-shrink: 0;

  border-radius: 10px;
  border: 1px solid var(--Line_200);
  background: var(--Bg_Wh);
`;

export const FontWrapper = styled.div`
  color: var(--Pri_500);
`